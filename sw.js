const CACHE='lunch-food-log-pwa-v3';
const APP_SHELL=['./','./index.php','./health.php','./backup.php','./reports.php','./manifest.json','./assets/app.css','./assets/meal-alert.js','./assets/icon.svg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(c=>c.addAll(APP_SHELL)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('lunch-food-log-pwa-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const r=event.request;if(r.method!=='GET')return;const u=new URL(r.url);if(u.origin!==self.location.origin)return;if(r.mode==='navigate'){event.respondWith(fetch(r).then(res=>{if(res.ok){const copy=res.clone();caches.open(CACHE).then(c=>c.put(r,copy));}return res;}).catch(()=>caches.match(r).then(c=>c||caches.match('./index.php'))));return;}event.respondWith(caches.match(r).then(c=>c||fetch(r).then(res=>{if(res.ok){const copy=res.clone();caches.open(CACHE).then(c=>c.put(r,copy));}return res;})));});
self.addEventListener('message',event=>{if(event.data?.type==='SHOW_MEAL_NOTIFICATION'){const meal=event.data.meal||'Meal';event.waitUntil(self.registration.showNotification(`🍽️ ${meal} Time`,{body:`It's time for ${meal}. Don't forget to record your food.`,icon:'./assets/icon.svg',badge:'./assets/icon.svg',tag:`meal-${meal}`,renotify:true,vibrate:[300,150,300,150,500],requireInteraction:true,data:{url:'./index.php'},actions:[{action:'open',title:'Open Food Log'},{action:'dismiss',title:'Dismiss'}]}));}});
self.addEventListener('notificationclick',event=>{event.notification.close();if(event.action==='dismiss')return;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list)if('focus' in client)return client.focus();return clients.openWindow(event.notification.data?.url||'./index.php');}));});
